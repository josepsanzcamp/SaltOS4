
# we are creating colorized themes derived from bootstrap

npm install bootstrap

rm -f dist/*.min.css

for i in blue purple orange green cyan pink gray slate crimson teal amber indigo; do
    sass scss/$i.scss dist/bootstrap.$i.min.css --style=compressed --no-source-map --load-path=.
done

rm -rf node_modules
rm -f package*.json
